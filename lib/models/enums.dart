enum SnackType {
  info,
  loading,
  success,
  error;

  // Color get color => switch (this) {
  //       info => AppTheme.backgroundColor,
  //       success => Colors.green.shade300,
  //       error => Colors.red.shade300,
  //     };
}
